spring:
  datasource:
    url: jdbc:h2:mem:tc2
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

team:
  connect:
    files:
      max-size: ${MAXSIZE:5MB}
